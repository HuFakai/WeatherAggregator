WeatherAggregator - 多源天气数据聚合服务开发规约 (V4.0 Final)1. 项目概述本项目是一个基于 Python 的高可用天气数据聚合系统。系统负责维护城市列表，通过多渠道（百度、高德、和风等）、多 API Key 轮询的方式定时抓取未来多天的天气预报数据。数据经过深度清洗（标准化）后，按渠道隔离存储，对外提供统一的 RESTful API。2. 技术栈 (Tech Stack)编程语言: Python 3.10+Web 框架: FastAPI (高性能 API 服务)异步任务: Celery 5.x (配合 Redis 做 Broker)数据库: MongoDB (存储异构天气数据)缓存/计数: Redis (用于 Key 限流计数、Celery 队列)HTTP 客户端: Httpx (异步) 或 Requests (根据 Celery 模式决定)部署环境: Docker Compose (适配 1Panel)3. 核心业务逻辑3.1 多 Key 随机负载均衡 (Random Load Balancing)为解决单 Key 限额问题，系统必须实现智能 Key 管理：Key 池管理: 每个渠道配置多个 Key。状态过滤: 每次调用前，过滤掉“已禁用”或“Redis 中标记今日已耗尽”的 Key。随机策略: 从可用 Key 列表中使用 random.choice() 随机选取。自动故障转移: 如果 API 返回配额不足/鉴权失败，立即在 Redis 标记该 Key 失效，并从剩余 Key 中重试，直到成功或全军覆没。3.2 数据抓取与清洗 (ETL)模式: 预报模式 (Forecast Mode)。抓取目标是“未来天气预报”而非单纯的“实况”。时间范围: 每次抓取必须包含 今天 (Today) 和 明天 (Tomorrow) 的数据（视 API 返回情况，多多益善）。标准化: 所有渠道的原始 JSON 必须转换为统一的 StandardDailyWeather 模型。3.3 存储策略 (Channel-Based Storage)结构: 不按照日期合并，而是按照渠道隔离存储。更新方式: 覆盖更新。每次抓取成功后，直接替换该城市下该渠道的整个数据列表。4. 数据库设计 (MongoDB Schema)4.1 cities (城市基础信息){
  "_id": "110105",          // Adcode (主键)
  "name": "朝阳区",
  "is_active": true,        // 抓取开关
  "mappings": {             // 各渠道对应的查询ID
    "baidu": "110105",
    "amap": "110105",
    "qweather": "101010100"
  }
}
4.2 channel_configs (渠道配置与 Key 池){
  "_id": "baidu",
  "base_url": "[https://api.map.baidu.com/](https://api.map.baidu.com/)...",
  "cron": "0 */3 * * *",    // Cron 表达式
  "keys_pool": [
    { "key": "SECRET_A", "daily_limit": 5000, "status": "active", "remark": "Account1" },
    { "key": "SECRET_B", "daily_limit": 5000, "status": "active", "remark": "Account2" }
  ]
}
4.3 weather_records (核心数据表){
  "_id": "110105",
  "city_name": "朝阳区",
  "last_updated_at": "2023-10-27T09:00:00Z",
  
  // 核心：按渠道 Key 存储预报列表
  "sources": {
    "baidu": [
      {
        "date": "2023-10-27",
        "temp_high": 25, "temp_low": 15,
        "weather_day": "晴", "weather_night": "多云",
        "humidity": 60,
        "aqi": 80, "aqi_level": "良",
        "lifestyle": [{"title": "穿衣", "level": "舒适", "desc": "..."}]
      },
      { "date": "2023-10-28", ... } // 明天的数据
    ],
    "amap": [ ... ]
  }
}
5. Pydantic 数据模型 (Python Code Reference)请严格遵循此模型定义，这是系统数据交换的标准。from typing import List, Dict, Optional
from pydantic import BaseModel, Field

# 1. 生活指数 (嵌套对象)
class LifestyleIndex(BaseModel):
    title: str = Field(..., description="指数名称，如穿衣、运动")
    level: str = Field(..., description="等级，如适宜、较差")
    desc: str = Field(..., description="详细建议")

# 2. 单日标准天气 (全量字段)
class StandardDailyWeather(BaseModel):
    date: str               # 格式 YYYY-MM-DD
    # 温度 (拆分高低)
    temp_high: int
    temp_low: int
    # 天气 (拆分昼夜)
    weather_day: str        # 映射后的标准描述 (晴/多云/雨/雪/阴)
    weather_night: str
    # 详细气象
    humidity: int           # 百分比 0-100
    wind_direction: str     # 如 "东南风"
    wind_scale: str         # 如 "3-4级"
    # 空气质量 (允许为空，部分渠道可能无数据)
    aqi: Optional[int] = None
    aqi_level: Optional[str] = None
    # 生活指数
    lifestyle: List[LifestyleIndex] = []

# 3. 数据库文档模型
class WeatherRecord(BaseModel):
    id: str = Field(alias="_id") # Adcode
    city_name: str
    last_updated_at: str
    # Key是渠道名(baidu), Value是该渠道的未来天气列表
    sources: Dict[str, List[StandardDailyWeather]] = {}
6. API 接口定义GET /api/v1/weather/{adcode}功能: 获取该城市所有渠道的聚合天气。返回: 直接返回 WeatherRecord 结构。POST /api/v1/admin/channel/keys功能: 动态添加/删除 API Keys。7. 开发分步指令 (Prompt Guide)请按照以下顺序向 AI 发出指令，以确保代码质量。
