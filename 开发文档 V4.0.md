# WeatherAggregator - 多源天气数据聚合服务开发规约 (V4.5 Architecture)

## 1. 项目概述

本项目是一个基于 Python 的高性能、高可用天气数据聚合与分发系统。系统负责维护全国城市库，通过多渠道（和风天气、易客天气、百度地图等）、多 API Key 轮询容灾的方式定时抓取未来多天的天气预报。数据经深度标准化清洗后，按渠道隔离存储在 MongoDB 中，对外通过 FastAPI 提供高性能 RESTful API，并提供现代化暗色玻璃拟态管理后台。

---

## 2. 技术栈 (Tech Stack)

- **编程语言**: Python 3.10+
- **Web 框架**: FastAPI 0.104+ (基于 Starlette 与 AnyIO，支持极高并发)
- **数据建模与验证**: Pydantic V2 (`model_dump` 序列化标准)
- **异步任务与分布式调度**: Celery 5.3+ (以 Redis 为 Broker，支持动态热重载)
- **数据库**: MongoDB 4.6+ (公网直连，按渠道隔离存储城市异构天气快照)
- **缓存与生命周期管理**: Redis 5.0+ (生命周期治理，全局绑定 TTL 自动驱逐)
- **HTTP 客户端**: Requests / Httpx (带连接池重用)
- **部署环境**: Docker Compose 容器编排 (无缝适配 1Panel 运维平台)

---

## 3. 核心架构与业务逻辑

### 3.1 多 Key 随机负载均衡与容灾轮转
为突破三方 API 每日限额壁垒，系统实现动态 Key 池管理：
- **Key 池管理**: 每个渠道均可配置独立的多 Key 池，支持每日请求限额设定。
- **状态与配额过滤**: 每次调用前，过滤掉状态非 active 或今日调用已达到上限的 Key。
- **随机选取策略**: 从可用 Key 列表中随机选取，均衡分配各 Key 的负载。
- **自动故障转移 (Failover)**: 若 API 返回配额耗尽、签名错误或鉴权失败，立即标记该 Key 失效并自动获取新 Key 重试，保证数据拉取成功率。

### 3.2 工厂化抓取器架构 (FetcherRegistry)
- 抓取器采用插件式单例工厂模式，所有实现继承自 `BaseFetcher`。
- 新增或集成数据源使用类装饰器 `@FetcherRegistry.register("channel_name")` 自动注入，上层 Celery 任务和实时聚合引擎无需关心具体的 Fetcher 类名与实例化细节。
- 抓取器统一实现：
  1. `fetch(city_id)`: 负责网络请求、参数映射与自动重试。
  2. `normalize(raw_data)`: 负责将三方异构 JSON 转换为标准的 `StandardDailyWeather` 模型列表。

### 3.3 Redis 全生命周期管理与数据治理
为防止 Redis 中产生累积无用数据，系统对全部键引入了严苛的生命周期控制：
- **渠道调用统计**: Key 格式为 `weather:stats:{channel}:{date}`，写入时通过 Pipeline 自动绑定 3 天 TTL (`expire(259200)`)。
- **客户端 QPM 限流**: Key 格式为 `weather:rate:min:{key}:{minute}`，通过 Pipeline 自动绑定 65 秒 TTL (`expire(65)`)，时间窗口滑动后自动被 Redis 回收。
- **Celery 任务元数据**: 显式设置 `task_ignore_result = True`，杜绝 `celery-task-meta-*` 在 Redis 中长期累积。
- **客户端 Key 迁移**: 当管理员在后台自定义修改 API Key 时，后台通过 Pipeline 自动将过去 7 天的每日调用统计键平滑迁移至新 Key，并继承原剩余 TTL，确保监控报表无缝过渡。

### 3.4 Celery Beat 动态调度器 (DynamicMongoScheduler)
- 自定义 `DynamicMongoScheduler` 继承自 `celery.beat.Scheduler`。
- **热重载机制**: 调度器在心跳 `tick()` 中每 5 秒检测一次 Redis 信号键 `weather:beat:reload_flag`。
- **零重启生效**: 管理员在管理后台调整渠道 Cron 表达式、启用/停用渠道或修改防风控延迟时，API 自动向 Redis 写入最新信号，Beat 调度器在 5 秒内自动加载新配置并更新内存调度，**完全无需重启 Celery 容器或进程**。

### 3.5 存储策略 (Channel-Based Storage)
- **渠道隔离**: 数据按渠道隔离存储于 `weather_records.sources.{channel_name}`。
- **原子覆盖**: 每次抓取成功后，原子性替换该城市下该渠道的未来预报列表，避免由于局部更新导致数据不一致。

---

## 4. 数据库设计 (MongoDB Schema)

### 4.1 cities (城市基础信息表)
```json
{
  "_id": "110100",
  "name": "北京",
  "province": "北京",
  "is_active": true,
  "mappings": {
    "hefeng": { "id": "101010100" },
    "yiketianqi": { "id": "110100" },
    "baidu": { "id": "110100" }
  },
  "created_at": "2025-12-01T00:00:00Z"
}
```

### 4.2 channel_configs (渠道配置与 Key 池表)
```json
{
  "_id": "hefeng",
  "name": "和风天气",
  "cron": "*/30 * * * *",
  "is_active": true,
  "random_delay": 10,
  "keys_pool": [
    {
      "key": "PROJECT_ID|API_KEY",
      "daily_limit": 2000,
      "status": "active",
      "desc": "生产主账号"
    }
  ]
}
```

### 4.3 client_keys (客户端鉴权密钥表)
```json
{
  "_id": ObjectId("..."),
  "key": "ck_live_9a8b7c6d",
  "name": "移动端 App 客户端",
  "qpm_limit": 60,
  "ip_whitelist_enabled": false,
  "ip_whitelist": ["127.0.0.1"],
  "created_at": "2026-09-18T12:00:00Z"
}
```

### 4.4 weather_records (天气核心聚合表)
```json
{
  "_id": "110100",
  "city_name": "北京",
  "adcode": "110100",
  "last_updated_at": "2026-09-18T22:15:00+08:00",
  "sources": {
    "hefeng": [
      {
        "date": "2026-09-18",
        "temp_high": 26,
        "temp_low": 16,
        "weather_day": "晴",
        "weather_night": "多云",
        "humidity": 45,
        "wind_direction": "南风",
        "wind_scale": "1-2级",
        "aqi": 55,
        "aqi_level": "良",
        "lifestyle": [
          { "title": "紫外线指数", "level": "中等", "desc": "适宜防晒" }
        ]
      }
    ],
    "yiketianqi": [ ... ],
    "baidu": [ ... ]
  }
}
```

---

## 5. Pydantic 数据模型 (V2 规约)

```python
from typing import List, Dict, Optional
from pydantic import BaseModel, Field

class LifestyleIndex(BaseModel):
    """生活指数模型"""
    title: str = Field(..., description="指数名称，如穿衣、运动、感冒")
    level: str = Field(..., description="等级描述，如适宜、较差")
    desc: str = Field(..., description="详细指导建议")

class StandardDailyWeather(BaseModel):
    """单日标准化预报模型"""
    date: str               # 格式: YYYY-MM-DD
    temp_high: int          # 最高温 (摄氏度)
    temp_low: int           # 最低温 (摄氏度)
    weather_day: str        # 白天天气状况
    weather_night: str      # 夜间天气状况
    humidity: int           # 相对湿度 (0-100)
    wind_direction: str     # 风向
    wind_scale: str         # 风力等级
    aqi: Optional[int] = None
    aqi_level: Optional[str] = None
    lifestyle: List[LifestyleIndex] = []

class WeatherRecord(BaseModel):
    """对外响应与入库文档模型"""
    id: str = Field(alias="_id")
    city_name: str
    last_updated_at: str
    sources: Dict[str, List[StandardDailyWeather]] = {}
```

---

## 6. API 接口规范

### 6.1 获取城市聚合天气
- **端点 1 (Path 传参)**: `GET /api/v1/weather/{city}`
- **端点 2 (Query 传参)**: `GET /api/v1/weather?city={city}`
- **认证方式**:
  - Header: `X-API-Key: {client_key}`
  - Query: `?key={client_key}`
- **响应体**: 返回 `WeatherRecord` 结构。

### 6.2 管理后台接口
- `POST /api/v1/admin/login`: 超级管理员身份鉴权。
- `GET/POST/PUT/DELETE /api/v1/admin/keys`: 客户端 Key 增删改查（PUT 支持 `new_key` 自定义修改并触发历史统计平滑迁移）。
- `GET /api/v1/admin/channels`: 获取数据渠道运行状态、调用统计与定时调度。
- `PUT /api/v1/admin/channels/{channel_name}/schedule`: 修改渠道定时任务 Cron、启停状态及随机延时，自动推送 Redis 热重载信号。
- `POST /api/v1/admin/channels/{channel_name}/keys`: 为指定渠道添加第三方 API Key。
- `DELETE /api/v1/admin/channels/{channel_name}/keys`: 移除第三方 API Key 并同步清理 Redis 统计。
- `POST /api/v1/admin/channels/{channel_name}/update`: 后台触发全量城市更新任务。

---

## 7. 质量保证与自动化测试

项目在 `tests/` 目录下配有覆盖全部核心业务的自动化测试用例：
```bash
# 执行全部核心优化与规范测试套件
python -m unittest tests/test_optimizations.py

# 执行定时任务规则解析测试
python tests/test_cron_schedule.py
```
- 每次发布前必须确保所有测试用例 100% 通过。
